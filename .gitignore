# Generated design-system sync bundle — copies of the canonical assets/.
# Regenerate with design-system/build.sh; never edit or commit these.
design-system/gf-tokens.css
design-system/gf-nav.js

# Dev-only Babel install for the Lost Triangle JSX transpile (assets/lost-triangle/build.mjs).
# Installed transiently with `npm install --no-save @babel/standalone`; never commit.
node_modules/

# design reference archives (not page assets)
vector-pod/*.zip

# Render output, capture scratch, and local agent dirs — never commit (multi-GB frame trees)
GREGG_MOTION_GRAPHICS/
output/
.playwright-cli/
.agents/
.DS_Store
