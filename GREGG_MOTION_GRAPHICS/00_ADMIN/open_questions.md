# Open Questions — resolve before polished rendering

Anything geometric not explicitly supported by Gregg's narration is flagged
`VERIFY_WITH_YUTO_GREGG`.

1. **INS_03 square derivation `VERIFY_WITH_YUTO_GREGG`** — the central insert.
   The exact operation by which the Lost Triangle + drawn sequence *produces*
   the square is not yet established from the audio (untranscribed). Must be
   pinned down (duplicate? mirror? rotate? boundary-trace?) before modeling —
   the square must be visibly *derived*, never dropped in as an overlay.
2. **Stray black blips (4:10, 4:18, 9:35)** — keep as black, or do they want
   graphics too? Currently assumed dropped-frame/edit artifacts.
3. **INS_04 / INS_06 durations** — masters should use the *measured* out-points
   (02:33.453 / 03:17.464), ~0.1 s longer than the handoff CSV. Confirm.
4. **Render target** — After Effects is unavailable in the automated env.
   Approve either (a) Blender-only headless, or (b) HTML/Canvas via the site's
   existing engine, as the production renderer.
