# The 30 MB master is provided separately; re-drop it here to reproduce extracts.
01_SOURCE/video/
# Heavy render outputs (regenerate locally) — but 05_RENDERS/review/ IS
# committed: those are the blocking-animatic deliverables for review.
05_RENDERS/previews/
05_RENDERS/frames/
05_RENDERS/**/*.mov
05_RENDERS/**/*.mp4
!05_RENDERS/review/*.mp4
# …except the full review cut: 126 MB, over GitHub's limit. Rebuild it with
# 00_ADMIN/build_review_cut.sh (needs the source master + the six masters).
05_RENDERS/review/REVIEW_CUT.mp4

# 07_DELIVERY/: docs + the small graphics string-out ARE committed; the heavy
# masters (ProRes) and the full timecode version are rebuild-only
# (00_ADMIN/build_delivery.sh). Over GitHub's file limit otherwise.
07_DELIVERY/masters_prores/
07_DELIVERY/review_timecode/
05_RENDERS/review/REVIEW_CUT_HD.mp4
